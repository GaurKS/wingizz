const channelType = {
  SOCIETY: 'society',
  WING: 'wing'
}

const requestType = {
  ADMIN: 'admin',
  USER: 'user'
}

const requestStatus = {
  PENDING: 'pending',
  APPROVED: 'approved',
  REJECTED: 'rejected'
}

const postType = {
  INVITE: 'invite',
  EVENT: 'event',
  POLL: 'poll'
}

module.exports = {
  channelType,
  requestType,
  requestStatus,
  postType,
}